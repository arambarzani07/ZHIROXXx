name: temp-v11-probe
on:
  push:
    branches: [temp-kanychnar-v11-exe-build]
jobs:
  probe:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe
        shell: pwsh
        run: |
          Write-Host "Windows runner OK"
          [Environment]::OSVersion.VersionString
